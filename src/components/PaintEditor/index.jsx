import React from 'react';
import styles from './paint.module.scss';
import classNames from 'classnames';
// import ClipCCPaintEditor from 'clipcc-paint';

const PaintEditor = () => {
    return (
        <div className={styles.container}>
            {/*<ClipCCPaintEditor />*/}
        </div>
    );
};

export default PaintEditor;
